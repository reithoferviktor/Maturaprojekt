<UserControl x:Class="Gokumoku.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Gokumoku"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListBox ItemsSource="{Binding Path=cells}" SelectionChanged="ListBox_SelectionChanged" x:Name="lb">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid></UniformGrid>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                
                <DataTemplate>
                    
                    <Ellipse x:Name="el" MinWidth="40" MinHeight="40">
                        
                    </Ellipse>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding Type}" Value="oppField">
                            <Setter TargetName="el" Property="Fill" Value="Blue"></Setter>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Type}" Value="ownfield">
                            <Setter TargetName="el" Property="Fill" Value="BlueViolet"></Setter>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Type}" Value="nonefield">
                            <Setter TargetName="el" Property="Fill" Value="Green"></Setter>
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
